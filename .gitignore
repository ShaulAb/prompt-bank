node_modules
.cursor/
dist/
.vscode/
checklists
*.vsix
reset-vscode-view.sh

# Debugging artifacts (top-level only)
/*.png

# Supabase CLI temporary files
supabase/.temp/
*.vsix

# Test/development prompt storage files (prevent accidental commits)
/prompts.json
/metadata.json
/sync-state.json

# Internal implementation plans (not for version control)
*_PLAN.md
*_PLAN*.md
